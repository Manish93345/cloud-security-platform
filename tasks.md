PHASE 0
    STEP 1: Account setup
        Github account -> DONE ✅
        AWS account -> PENDING ❌
        VERCEL / NETLIFY -> DONE ✅

    STEP 2: Local Setup
        Install Python 3.10+ -> PENDING ❌
        Install pip + virtualenv -> PENDING ❌
        Install node.js + npm (React frontend) -> PENDING ❌
        Install GIT -> PENDING ❌

    STEP 3: Basic Practice
        AWS CLI install and configure -> PENDING ❌
            AWS access key + secret daloge → phir tum CLI se S3, EC2 access kar paoge
        Python mein boto3 install karna -> PENDING ❌
            pip install boto3
            Aur ek chhota script likhkar test karo ki tumhare AWS S3 buckets list ho rahe hain ya nahi.
        
    STEP 4: GitHub flow
        Github repo (cloud-security-platform) -> DONE ✅
        CODE PUSH -> REGULARLY ⚛️
