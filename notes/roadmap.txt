Cloud Security Automation Platform


# Phase A – Foundation

Day 1 – Environment Setup
    Day 1 Completed:
    - Project folder created
    - Python environment ready
    - Virtual environment set
    - Git initialized

DAY 2
    STEP 1: AWS Account Safety First (10 min)
        AWS Console login karo
        Billing → Budgets:
        Budget set karo (₹0 / $0 alert)
        Email alert on
        Isse peace of mind milta hai baby 😌
    STEP 2: IAM — Dil aur Dimag dono se samjho 🧠🔐
        IAM ka simple matlab:
        User = tum (tool)
        Permission = kya dekh sakta hai
        Policy = rules
        Hum READ-ONLY permission denge.
        No delete, no modify. Safe & professional.
    STEP 3: IAM User Create Karo (Step-by-step)
        IAM → Users → Create user
        Username: cloud-scanner-user
        Access type:
        ✅ Programmatic access (Access Key)
        Permissions:
        Attach policy directly
        Choose: ReadOnlyAccess
        Create user
        👉 End mein:
        Access Key ID
        Secret Access Key
        ⚠️ Important baby:
        Inko notes mein likh lo
        GitHub pe kabhi upload mat karna ❌
    STEP 4: AWS CLI Install & Configure (15 min)
        Install AWS CLI (Windows)
        AWS official site se install karo
        Check:
        aws --version
        Configure:
        aws configure
        Fill karo:
        Access Key
        Secret Key
        Region: ap-south-1 (India)
        Output format: json
    STEP 5: First AWS Test (Confidence Booster 💪)
        Command run karo:
        aws iam list-users
        Agar output aaya → AWS + CLI perfectly connected 🎉
    
    Day 2 Completed:
        - AWS account secured with budget alert
        - IAM read-only user created
        - AWS CLI configured
        - First AWS command tested successfully


DAY 3
    STEP 1: boto3 install karo
    STEP 2: First AWS Python Script (S3 Test)
    STEP 3: Script Run karo
    STEP 4: Git Commit karo

DAY 4: Security Groups ko programmatically read karna aur samajhna.
    Security Group = firewall rules for EC2 servers
        Port 22 open to 0.0.0.0/0 ❌ (dangerous)
        Port 80 open to world ✅ (web server case)
    Hum kya karenge?
        AWS se security groups read karenge
        Check karenge koi rule duniya ke liye open toh nahi

    STEP 1: EC2 Client Script banao
    STEP 2: Run karo
    STEP 3: Intentionally Test Karna (Optional but Powerful)
    STEP 4: Git Commit

    created: test_security_groups.py -> Read security groups, check inbound rules, 


DAY 5: HIGH RISK DETECTOR
    Script ko modify karenge taaki:
        Agar 22 open to 0.0.0.0/0 →Console pe likhe:[HIGH RISK] SSH exposed to entire internet
    
    Updated test_security_groups.py

DAY 6: Generic Internet Exposure Detector (Agar koi bhi port duniya ke liye open hai → flag karo)
    Improving test_security_groups.py
        Detects ALL internet exposure
        Classifies risk level
        Detects multiple sensitive ports
        More realistic logic


DAY 7: Structured JSON Security Report generate karna.
    New File banao :-> backend/security_scanner.py
    I built a cloud security scanner that detects internet-exposed ports and generates structured risk reports.

    Till Now:
        Identify karta hai vulnerable resource
        Exact port batata hai
        Protocol batata hai
        Exposure range batata hai
        Risk level classify karta hai
        JSON structured output deta hai

PROFESSIONAL TOUCH START: Professional Report + Summary Engine banana
    Created report_writer.py
    Modified security_scanner.py
    Run python backend/security_scanner.py
    A report will be generated security_report.json in reports folder

    Now I have:
        Data Collection Layer
        Risk Classification Layer
        Reporting Layer


S3 Public Bucket Detection
    STEP 1: New Scanner File Banao
        backend/
            scanners/
    s3_scanner.py

    STEP 2: Integrate in main scanner   security_scanner.py


Making clean Architecture
    backend/
        scanners/
            ec2_scanner.py
            s3_scanner.py
        core/
            report_writer.py
        main.py



PHASE B

STEP 1: Flask API Banana
    pip install flask
    backend/api.py file banao
    http://127.0.0.1:5000/  
    http://127.0.0.1:5000/scan  -> it gives the report json 
    I built a web-based cloud security automation API that scans AWS infrastructure.
    Browser → Flask API → Scanner Engine → AWS → JSON Response

NEXT STEP (Strategic & Professional Move) -> Add Timestamped Reports + Scan History
    Har scan ka alag record
    History maintain
    Future dashboard ke liye base ready

    report_writer.py Update
    python backend/main.py
    python backend/api.py
    Check reports folder
        security_report_2026-02-13_22-31-05.json
    The system maintains timestamped scan history for audit and future dashboard integration.


Next Step: Add Scan Trigger Control + Scan History API
    Abhi kya hai?
        /scan run karta hai
        Report save hoti hai
    Par user:
        Past scans dekh nahi sakta
        History fetch nahi kar sakta

    /history endpoint
    /report/<filename> endpoint

SWITCH TO DJANGO WHEN WORKING WITH USER ACCOUNT AUTHENTICATION AND DATABASES


NEXT STEP: FRONTEND LAYER
    Ab tak: Browser → Flask API → AWS → JSON → File
    Ab hum banayenge: Browser UI → API call → Live Result Screen

    backend/
        templates/
            index.html
    python backend/api.py







PHASE C — PROPER SaaS ARCHITECTURE START


    Root Structure
    root/
        backend/
        frontend/
        reports/

Created a basic frontend and checking the scan result
Connected backend and frontend

Multi-Page Dashboard + Scan History Page
    Left sidebar
    Multiple pages
    Navigation
    History view
    Detailed report view

    /dashboard
    /history
    /report/:id


    React Router Install Karo: npm install react-router-dom

    tumne build kiya hai:
        Modular Flask backend
        Multi-service AWS scanner (EC2 + S3)
        Risk classification engine
        Timestamped reporting
        History API
        React + Vite frontend
        Multi-page routing
        Dashboard + History separation


SHIFTING TO DJANGO
    created backend_django/
        django-admin startproject cloudsaas
   
        backend_django/
        │
        ├── cloudsaas/          ← project root
        │   ├── manage.py       ← command runner
        │   └── cloudsaas/      ← actual Django config package
        │       ├── settings.py
        │       ├── urls.py
        │       ├── asgi.py
        │       └── wsgi.py

        cd backend_django/cloudsaas : -> python manage.py startapp scanner


(Django Phase 1)
    activate venv 
    pip install django djangorestframework
    backend_django/cloudsaas/cloudsaas/settings.py
        INSTALLED_APPS = [
            add: 'rest_framework',
                    'scanner',
    python manage.py migrate


        python manage.py runserver
            React (5173)
            ↓
        Django REST API (8000)
            ↓
        Scanner Engine (services/)
            ↓
        AWS
            ↓
        Report JSON

    pip install django-cors-headers




NEXT BIG PHASE

Authentication + Multi-User SaaS Start
    Register
    Login
    JWT token
    Protected scan API
    Per-user scan history (DB stored)

    pip install djangorestframework-simplejwt


NEXT PHASE
    Frontend Login Integration (Real SaaS Mode)
        



